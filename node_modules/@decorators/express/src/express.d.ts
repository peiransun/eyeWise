/// <reference types="express" />
import { Express } from 'express';
import { InjectionToken } from '@decorators/di';
import { Type } from './middleware';
/**
 * Error Middleware class registration DI token
 */
export declare const ERROR_MIDDLEWARE: InjectionToken;
/**
 * Attach controllers to express application
 *
 * @param {Express} app Express application
 * @param {Type[]} controllers Controllers array
 */
export declare function attachControllers(app: Express, controllers: Type[]): void;
